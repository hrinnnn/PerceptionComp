# Videos

This directory is the intended location for benchmark videos.

The current repository snapshot does not yet bundle the video files themselves. Annotation entries reference videos by `video_id`, and the expected mapping is:

```text
benchmark/videos/<video_id>.mp4
```

If videos are hosted externally in a later release, this directory can be populated manually or via a download script.
